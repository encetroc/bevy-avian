# Issue tracker

This repository uses local Markdown files for issue tracking.

## Location

Create each issue under:

```text
.scratch/<feature>/
```

Keep the issue description and related working notes in that feature directory. Use descriptive feature names and preserve the files as the local source of truth for work items.

There is no remote issue tracker or CLI workflow configured for this repository.
